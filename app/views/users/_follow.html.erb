<% if user.followed_by?(current_user) %>
  <%= link_to 'フォロー中', user_relation_path(user.id, user.id), method: :delete, remote: true, class:"btn btn-success" %>
<% else %>
  <%= link_to 'フォロー', user_relations_path(user.id), method: :post , remote: true, class:"btn btn-primary"%>
<% end %>